% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{neogro}
\alias{neogro}
\title{Solve the Neoclassical Growth Model}
\usage{
neogro(
  NK = 200L,
  MAXIT = 500L,
  TOL = 1e-05,
  TOL1 = 1e-07,
  NEG = -1e+10,
  ZETA = 0.05,
  beta = 0.994,
  sigma = 2,
  alpha = 0.28,
  delta = 0,
  verbose = FALSE
)
}
\arguments{
\item{NK}{Integer. Number of grid points for capital (default = 200).}

\item{MAXIT}{Integer. Maximum number of value function iterations (default = 500).}

\item{TOL}{Numeric. Convergence tolerance for value function (default = 1e-5).}

\item{TOL1}{Numeric. Convergence tolerance for golden section search (default = 1e-7).}

\item{NEG}{Numeric. Large negative number used as a placeholder for infeasible utility (default = -1e10).}

\item{ZETA}{Numeric. Relaxation parameter for value function updates (default = 0.05).}

\item{beta}{Numeric. Discount factor \eqn{\beta} (default = 0.994).}

\item{sigma}{Numeric. Relative risk aversion (CRRA) parameter \eqn{\sigma} (default = 2.0).}

\item{alpha}{Numeric. Capital share \eqn{\alpha} in production (default = 0.28).}

\item{delta}{Numeric. Depreciation rate of capital \eqn{\delta} (default = 0.0).}

\item{verbose}{Boolean. If \code{true} prints the error at each iteration (default = false).}
}
\value{
A \code{DataFrame} with the following components:
\itemize{
\item \code{k} – Capital stock.
\item \code{v} – Value function.
\item \code{kopt} – Optimal capital choice.
\item \code{copt} – Optimal consumption choice.
\item \code{iopt} – Optimal investment choice.
\item \code{y} – output.
\item \code{ropt} – residuals of first-order condition of optimal savings
decision problem..
}
}
\description{
This function solves a standard deterministic neoclassical growth model
using value function iteration. The solution returns the optimal
policy functions and the value function over a discretized grid of capital.
}
\details{
The model assumes a Cobb-Douglas production function
\eqn{f(k) = k^{\alpha}}, and solves for the household's
optimal savings policy under given parameters. Convergence is controlled
by the tolerances \code{TOL} and \code{TOL1}.
}
\examples{
# Solve the neoclassical growth model with default parameters
result <- neogro()
head(result)

# Solve with custom parameters
result <- neogro(NK = 300, beta = 0.96, alpha = 0.33, delta = 0.05)
plot(result$k, result$kopt, type = "l", main = "Policy Function")

}
